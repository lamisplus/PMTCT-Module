<?xml version="1.0" encoding="utf-8"?>
<databaseChangeLog
        xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
        xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
        xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-3.5.xsd">

    <property name="autoIncrement" value="true"/>

    <changeSet id="20222709-001" author="drkarim-data-fi">
        CREATE TABLE IF NOT EXISTS public.pmtct_anc
        (
        id bigint NOT NULL DEFAULT nextval('pmtct_anc_id_seq'::regclass),
        anc_no character varying(255) COLLATE pg_catalog."default",
        created_by character varying(255) COLLATE pg_catalog."default",
        created_date timestamp without time zone,
        date_registration date,
        facility_id bigint,
        hospital_number character varying(255) COLLATE pg_catalog."default",
        last_modified_by character varying(255) COLLATE pg_catalog."default",
        last_modified_date timestamp without time zone,
        uuid character varying(255) COLLATE pg_catalog."default",
        lmp date,
        gaweeks integer,
        gravida integer,
        parity integer,
        referred_syphilis_positive_client character varying(255) COLLATE pg_catalog."default",
        source_of_referral character varying(255) COLLATE pg_catalog."default",
        syphilis_test_result character varying(255) COLLATE pg_catalog."default",
        tested_for_syphilis character varying(255) COLLATE pg_catalog."default",
        treated_for_syphilis character varying(255) COLLATE pg_catalog."default",
        archived bigint,
        CONSTRAINT pmtct_anc_pkey PRIMARY KEY (id)
        )

        TABLESPACE pg_default;

        ALTER TABLE IF EXISTS public.pmtct_anc
        OWNER to postgres;
    </sql>
    </changeSet>

</databaseChangeLog>
